## Project Structure

- `services/proxy`: Proxy service to forward requests to OpenCode server. Created using Hono.
- `services/opencode`: Dockerfile for OpenCode server


## Running the project using Makefile

1. `make install`
2. `make build`
3. `make dev`

